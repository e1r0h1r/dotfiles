- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.vim/vimrc:
        path: vim/vimrc
        create: true
    ~/.zprofile: zprofile
    ~/.zshrc: zshrc
    ~/.config: config
    ~/.iterm2: iterm2
    ~/.oh-my-zsh: oh-my-zsh
    ~/.SpaceVim: SpaceVim

- shell:
  - [git submodule update --init --recursive, Installing submodules]

- clean: ['~']

- link:
    ~/.gitconfig: gitconfig


- clean: ['~']

- link:


