- defaults:
    link:
      relink: true

- clean: ['~']

- create: oh-my-zsh

- link:
    ~/.gitconfig: gitconfig
    ~/.vim/vimrc:
        path: vim/vimrc
        create: true
    ~/.zprofile: zprofile
    ~/.zshrc: 
        force: true
        path: zshrc
    ~/.iterm2:
        path: iterm2
        create: true
    ~/.oh-my-zsh/: oh-my-zsh

- shell:
  - [git submodule update --init --recursive, Installing submodules]

